[project]
name = "short-video-intelligence-dashboard"
version = "0.1.4"
description = "Interactive analytics for short-video growth, creators, and virality."
requires-python = ">=3.11"
readme = "README.md"
license = {file = "LICENSE"}

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
